---
title: go-gen-jsonschema
description: Agent-first documentation for generating deterministic, LLM-ready JSON Schema from Go types.
template: splash
hero:
  tagline: Go types in. LLM-ready JSON Schema out.
  actions:
    - text: Human docs
      link: /getting-started/
    - text: llms.txt
      link: /llms.txt
      variant: secondary
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import CopyCommand from '../../components/CopyCommand.astro';

<CopyCommand
  eyebrow="Agent quickstart"
  title="Install the skill, then tell your agent what to build"
  label="terminal"
  command="npx skills add tylergannon/go-gen-jsonschema"
/>

## Start with the task in front of you

<CardGrid>
  <Card title="Generate your first schema" icon="rocket" href="/getting-started/">
    Pin the Go tool, register a type, generate, validate, and commit the outputs.
  </Card>
  <Card title="Model missing and null" icon="open-book" href="/features/optionality/">
    Choose ordinary fields, `Optional[T]`, or `Nullable[T]` without guessing.
  </Card>
  <Card title="Enums and interfaces" icon="puzzle" href="/features/enums/">
    Teach tool schemas named values and discriminated implementations.
  </Card>
  <Card title="Validate and prevent drift" icon="setting" href="/guides/validation-ci/">
    Validate LLM output and make CI reject stale generated schemas.
  </Card>
</CardGrid>

## What the generator guarantees

- Property order follows Go struct field order, producing deterministic prompts.
- Unknown properties are rejected with `additionalProperties: false`.
- Go doc comments become LLM-facing schema descriptions.
- Ordinary and nullable fields are required; only `Optional[T]` removes a property from `required`.
- Generated schema files and Go accessors are safe to commit and verify in CI.
- Optionality, enums, discriminated interfaces, shared `$defs`, providers, and validation use explicit APIs.

Need exact exported signatures? The generated [Go API reference](/api/) is available,
but most integrations should begin with [Getting started](/getting-started/).
