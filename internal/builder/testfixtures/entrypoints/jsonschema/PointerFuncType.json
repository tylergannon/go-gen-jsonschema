{
  "type": "integer",
  "description": "PointerFuncType is a named pointer type. Go forbids declaring a method on it (invalid receiver base type), so its schema entrypoint must be registered as a free function."
}
