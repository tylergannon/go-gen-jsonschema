Generated union decoding previously lost discriminator information when values were marshaled again. Generate field-aware `MarshalJSON` alongside the containing struct's `UnmarshalJSON`, so `I`, `Optional[I]`, and direct `[]I` use the same resolved schema/encode/decode mapping.

The owner codec preserves registered pointer/value implementations, per-field discriminator keys and values, ordinary custom JSON hooks, and transactional replacement decoding. It rejects nil/unregistered union values, non-object or conflicting payloads, ambiguous legacy identities, and competing production owner methods before unsafe output. Owners with generated codecs must be nested through named fields rather than embedded where method promotion would compete.

The shared `OwnerCodec` structure is the extension point for #58 enum adapters. Updated fixtures, examples, website, and agent guidance describe owner-based encoding instead of requiring global discriminator methods on implementations.

Validation: `GOFLAGS=-p=2 go test ./...`, website build/link checks, generated consumer encode/validate/decode and full artifact idempotence proof passed. Independent consumer probes exposed pointer-hook addressability and null/empty discriminator defects; regressions and fixes are included. Independent review convergence is pending before merge.

Closes #57.
