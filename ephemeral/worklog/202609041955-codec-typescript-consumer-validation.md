# Codec and TypeScript consumer validation

decision: Build the validation fixture in an isolated branch from `origin/main` and execute it only against an explicitly supplied clean candidate path and exact commit. An older local binary or branch state cannot satisfy the proof.
decision: The fresh consumer exercises a single generated owner containing field-specific unions and enum modes, Optional and Nullable fields, two discriminator configurations for the same interface implementations, ordinary fields handled through the parent alias, schema validation, transactional decode failures, and TypeScript compilation against the generated wire model.
proof_plan: Build the candidate CLI, copy the canonical consumer to a disposable directory, add a temporary module replacement to the exact candidate, generate Go schemas/codecs and TypeScript, run uncached consumer tests, compile positive and negative TypeScript assignments with the pinned compiler, preserve generated outputs and exact command logs under `ephemeral/codec-ts-validation/runs/<candidate-sha>/`, and verify neither the candidate nor canonical fixture changed.
proof: Required preparation baseline `GOFLAGS=-p=2 go test ./...` passed at validation base `b2fd7ee189527c2cee0314614b75528e771bd15b`.
proof_prep: In a disposable copy, the current-main generator accepted the consumer registrations, generated the structural TypeScript, and TypeScript 6.0.3 compiled all positive and expected-negative assignments. This checks fixture readiness only; current main has no #57/#58 codecs and is not candidate evidence.
friction: First exact-candidate execution generated successfully, then `go test` stopped because generated validation code added a validator import that pre-generation `go mod tidy` could not discover. Preserve that attempt and run `go mod tidy` again after generation before testing the generated consumer.
proof: Exact candidate `5e1040a99ba7f51f126601bd84ad5e6b912847d2` passed the fresh consumer run. The built CLI generated Go/schema/TypeScript with empty stdout and stderr; uncached consumer tests logged schema-valid value/pointer-identical JSON, correct field-specific union discriminators, string-name and numeric enum modes, Optional/Nullable behavior, ordinary alias field behavior, semantic decode/re-encode, and four transactional decode rejections. Generated-source inspection observed one owner marshal/unmarshal pair, eight `RawMessage` special-field shadows, ordinary fields retained through the alias, sealed `Created`/`*Deleted` switches, and no global `State` codec. TypeScript 6.0.3 compiled positive wire values and expected negatives for wrong enum values, null Optional, and the alternate field's wrong discriminator.
proof_artifacts: `ephemeral/codec-ts-validation/runs/5e1040a99ba7f51f126601bd84ad5e6b912847d2/` contains exact stdout/stderr, generated outputs, tool/candidate provenance, binary and artifact hashes, and result status. Both canonical fixture and candidate tracked state were unchanged. The initial harness-ordering failure is retained beside it as the `-attempt1` run.
proof: Final validation-worktree gate `GOFLAGS=-p=2 go test -count=1 ./...` passed after the proof runner and artifacts were complete.
