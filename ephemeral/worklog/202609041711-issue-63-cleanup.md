# Issue #63 cleanup

decision: preserve the functioning provider API for v1 and document its schema-only/runtime-rendering boundary, based on the accepted contract in docs/spec/v1.md and the 2026-09-04 retention approval.

decision: delete tool_types.go because it is a `nobuild` file containing an unfinished Tool/BuildTool experiment with no shipped runtime path.

doc_bug: README.md, llms.txt, and website CLI reference listed the no-op `num-test-samples` flag -> removed the stale documentation while the CLI owner removes the flag implementation.

doc_bug: docs/tutorial.mdx and docs/design/v1-roadmap.md referenced deleted examples/v1 and unfinished work -> replaced current tutorial links and added a historical roadmap disposition without rewriting raw planning notes.

doc_bug: docs/providers-documentation.md described a provider rendering bug that current generated output and tests no longer reproduce -> rewrote it around the retained provider contract and current evidence.

finding: issue #10 has three unchecked items about generated UnmarshalJSON ownership/package boundaries; current v1 contract and generated examples cover selected local owner decoding, so leave the issue open for its remaining scope.

finding: issue #17 is superseded by #32's explicit Optional/Nullable design; do not treat its legacy `jsonschema:"optional"` proposal as supported.

finding: issue #28 is superseded by closed #32; its primitive-only proposal and deprecation language are obsolete, while #32's broader wrapper contract is the current target.

finding: issue #32 is closed and its implementation/docs/proof requirements are present on current main; retain it as historical provenance rather than treating it as pending work.

proof: baseline `GOFLAGS=-p=2 go test ./...` passed before edits.

decision: rebased the cleanup checkpoint onto `origin/main` at `604fabb` after the accepted-contract docs landed; the resulting cleanup commit is `22658ca`.

proof: post-rebase `GOFLAGS=-p=2 go test ./...` passed, `git diff --check` passed, and the focused stale-claim scan passed.

friction: `website` `npm run check` cannot build in this worktree because the available Node environment cannot resolve `astro/config`; its prebuild briefly regenerated API/fixture artifacts, which were restored before closeout. Link checking is unavailable because `website/dist` was not produced.

correction: the repository CI and `website/README.md` bless npm with the committed `package-lock.json`; `npm ci` provisioned dependencies successfully without changing the lockfile.

proof: after `npm ci`, `npm run check` passed: Astro built 13 pages and the link checker validated 17 HTML pages. The generated API index was restored afterward because the check refreshed unrelated source-comment output.
