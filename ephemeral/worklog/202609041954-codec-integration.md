# Codec integration worklog

## Scope

Integrate the reviewed Go enum and sealed-union codec work through `8ad91d5`
with `origin/main` at `b2fd7ee`, preserving the native TypeScript backend and
the accepted owner-wrapper architecture. Do not change the paused CLI,
preview, or inspection worktrees and do not push or merge externally.

## Baseline and setup

- In the clean issue-58 worktree at `8ad91d5`, ran
  `GOFLAGS=-p=2 go test ./...`; it passed and left the worktree clean.
- Fetched `origin/main` at `b2fd7ee189527c2cee0314614b75528e771bd15b`.
- Created `/Users/tyler/src/.worktrees/go-gen-jsonschema/v1-codec-integration`
  on `codex/v1-codec-integration` from that commit.
- `git merge-tree --write-tree --messages origin/main 8ad91d5` found two
  content conflicts: `internal/builder/gen_schema.go` and
  `internal/syntax/scan_result.go`.

## Decisions

- Keep main's `internal/syntax.ResolveEnum` and evaluated `EnumValue` as the
  one shared enum-membership representation for schema, Go codec, and
  TypeScript generation.
- Preserve reviewed issue-58 history in a local integration merge.
- Preserve the accepted plain-owner/RawMessage codec architecture, hook and
  promotion guards, and fresh per-render template projections.

## Commands and evidence

- Resolved the merge without changing the accepted codec architecture. The
  issue branch's declaration-syntax `ScanResult.EnumValues` helper was removed;
  `EnumFieldPlan` now projects directly from main's evaluated
  `syntax.EnumValue` members.
- Preserved main's `json.Number` support for enum schema integers, including
  values outside native `int`, and its existing description-free field-local
  enum schema behavior. The first focused run caught the description mismatch;
  the compatibility fix retained main's explicit TypeScript parity assertion.
- `GOFLAGS=-p=2 go test ./internal/syntax ./internal/builder` passed.
- `GOFLAGS='-count=1 -p=2' go test ./...` passed.
- One full `go generate ./...` removed four stale field descriptions inherited
  from the issue branch's example schemas. After staging those correct outputs,
  a second `go generate ./...`, `git diff --exit-code`,
  `JSONSCHEMA_NO_CHANGES=1 go generate ./...`, and a second
  `git diff --exit-code` all passed with no unstaged byte changes.
- `npm ci --ignore-scripts --no-audit --no-fund && npm test` in
  `tests/typescript` passed all eleven conformance claims. The retained run is
  `ephemeral/typescript-generation/proof/run-lG5XUd`.
- A fresh external consumer requires published `v1.0.0-rc.4` with no replace
  directive and was generated by this worktree's CLI. Repeated
  `gen --validate --no-changes` retained aggregate SHA-256
  `eaab0af4020b31b8132e2b7ddd3159af2bf426f290e64cfd2cd0c3ac2e7bed3b`;
  uncached tests passed value/pointer sealed-union and direct/numeric/Optional/
  Nullable enum schema-valid roundtrips, rejection, hook, and transactionality
  assertions. Source and transcript are under
  `ephemeral/codec-integration-consumer*`.
- The initial external-consumer test requested `go mod tidy` after generated
  validation imports were added. Tidying once, then regenerating before the
  stability hash, produced the clean proof above.

## Published branch ancestry

Published PR #74 head `6207608` and issue-58 head `8ad91d5` are sibling
descendants of `a7607da`; their union fix commits are patch-equivalent but the
published head is not an ancestor of `8ad91d5`. After the content integration
commit, record `6207608` as a merge parent without changing the reviewed tree so
the eventual PR update can fast-forward and does not discard published history.

## Final state

- Content integration merge: `abdbebb` (`b2fd7ee` + reviewed `8ad91d5`).
- Published PR #74 ancestry merge: `3e91975`; its `ours` strategy changed no
  files and makes `6207608` an ancestor of the candidate.
- After both commits, the complete sequence `go generate ./...`, clean
  `git diff --exit-code`, `JSONSCHEMA_NO_CHANGES=1 go generate ./...`, clean
  `git diff --exit-code`, and `GOFLAGS='-count=1 -p=2' go test ./...` passed.
- Branch `codex/v1-codec-integration` is clean. Nothing was pushed or merged
  externally.

## Closeout preparation

- User correction: the root agent remains management-only; lower agents own
  implementation, validation, proof capture, and operating-artifact updates.
- Integration adjudication: main's shared `syntax.ResolveEnum`, arbitrary-size
  numeric enum schema support, native TypeScript lowering, and established
  description behavior took precedence at the two source conflicts. The
  reviewed parent wrapper, enum adapters, union switches, fresh render
  projection, and collision/nil/hook guards were retained around that shared
  representation.
- Cherry-picked validator artifact-only commits `2851a3f` and `b8eb52f` as
  `35b1ba0` and `dbcc9f2`. The non-ephemeral product diff hash against main was
  unchanged before and after at
  `22a1e0c3e3c56543bfdf60bef4de21adb87fc4c6b598275ad61232bf34017b33`.
- Drafted the combined PR #74 title and body under
  `ephemeral/pr-74-combined-title.txt` and
  `ephemeral/pr-74-combined-body.md`; permanent evidence links use artifact
  commit `dbcc9f2` and the exact validated candidate `5e1040a`.
