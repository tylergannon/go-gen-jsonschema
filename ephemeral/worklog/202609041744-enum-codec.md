# Enum codec worklog

decision: Implement issue #58 from the corrected #57 checkpoint. A single resolved field enum plan owns schema values and any generated owner encode/decode adapter.
decision: Generate adapters only for integer-backed `WithStringerEnum` fields shaped as direct `E`, `Optional[E]`, or `Nullable[E]`; numeric integer enums and string-backed enums retain ordinary encoding.
decision: Wire strings come from Go constant identifiers, never `String()`. Duplicate integer values with distinct identifiers, unsupported string-mode containers, and production enum JSON hooks are generation errors before artifacts are written.

proof: Baseline `GOFLAGS=-p=2 go test ./...` passed at a7607da before issue #58 edits. The reviewed #57 convergence fix 6db8115 was then cherry-picked as 8e1942d before enum implementation.

review integration: #57 round-2 fixes were incorporated as fd8552a before final enum proof, preserving foreign embedded-owner collision rejection and pointer-only concrete hook encoding in the mixed union/enum fixture.
decision: Duplicate underlying values are rejected only for adapted integer string mode, where distinct constant names make encoding ambiguous. Numeric mode and ordinary string-backed modes deduplicate aliases without changing their wire meaning.
proof: Generated fixtures cover direct, Optional, and Nullable adapted enums; declared zero in every adapted shape; undeclared zero; negative/noncontiguous values; remote same-repository enums; a misleading String method that is never called; string-backed ordinary JSON; numeric per-field mode; runtime errors and transactional decode; and one owner composing union and enum fields.
proof: A fresh module requiring published v1.0.0-rc.3 markers, with no source replace, used the branch-built generator to encode, schema-validate, decode, and semantically compare a mixed union/enum owner through value and pointer calls. Every consumer file was byte-identical after a second generation; generated Config schema/source hashes were c5706dcc80352fb7309fab5f28f6ab7114103111f373f6c3d3aaa627c33c9283 and d4a8b82e9af48a3dbb77df1721edf31836be53c20162a98bbf0215b34f3023f7.
proof: `GOFLAGS=-p=2 go test ./...` passed after the final enum and #57 integration changes. Two full `GOFLAGS=-p=2 go generate ./...` runs preserved the complete staged repository diff byte-for-byte (`4806d883d1bb9a6873680f5782de3fcb879626ebec56136dc8680761f69dff0b`).
