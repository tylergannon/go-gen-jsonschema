# Enum codec worklog

decision: Implement issue #58 from the corrected #57 checkpoint. A single resolved field enum plan owns schema values and any generated owner encode/decode adapter.
decision: Generate adapters only for integer-backed `WithStringerEnum` fields shaped as direct `E`, `Optional[E]`, or `Nullable[E]`; numeric integer enums and string-backed enums retain ordinary encoding.
decision: Wire strings come from Go constant identifiers, never `String()`. Duplicate integer values with distinct identifiers, unsupported string-mode containers, and production enum JSON hooks are generation errors before artifacts are written.

proof: Baseline `GOFLAGS=-p=2 go test ./...` passed at a7607da before issue #58 edits. The reviewed #57 convergence fix 6db8115 was then cherry-picked as 8e1942d before enum implementation.

review integration: #57 round-2 fixes were incorporated as fd8552a before final enum proof, preserving foreign embedded-owner collision rejection and pointer-only concrete hook encoding in the mixed union/enum fixture.
decision: Duplicate underlying values are rejected only for adapted integer string mode, where distinct constant names make encoding ambiguous. Numeric mode and ordinary string-backed modes deduplicate aliases without changing their wire meaning.
proof: Generated fixtures cover direct, Optional, and Nullable adapted enums; declared zero in every adapted shape; undeclared zero; negative/noncontiguous values; remote same-repository enums; a misleading String method that is never called; string-backed ordinary JSON; numeric per-field mode; runtime errors and transactional decode; and one owner composing union and enum fields.
proof: A fresh module requiring published v1.0.0-rc.3 markers, with no source replace, used the branch-built generator to encode, schema-validate, decode, and semantically compare a mixed union/enum owner through value and pointer calls. Every consumer file was byte-identical after a second generation; generated Config schema/source hashes were c5706dcc80352fb7309fab5f28f6ab7114103111f373f6c3d3aaa627c33c9283 and d4a8b82e9af48a3dbb77df1721edf31836be53c20162a98bbf0215b34f3023f7.
proof: `GOFLAGS=-p=2 go test ./...` passed after the final enum and #57 integration changes. Two full `GOFLAGS=-p=2 go generate ./...` runs preserved the complete staged repository diff byte-for-byte (`4806d883d1bb9a6873680f5782de3fcb879626ebec56136dc8680761f69dff0b`).

parent integration: Updated README, llms, registration skill reference, website enum guide, and v1 matrix with the implemented direct/Optional/Nullable field codec boundary. Documented constant-name compatibility, numeric-mode preservation, ambiguous alias migration, unknown-value errors, transactional decode, and rejected adapted hooks/containers.
parent proof: Independent consumer requiring published v1.0.0-rc.3 (no replace) passed nested union plus direct/Optional/Nullable string enums with both owner call sites, negative/noncontiguous values, numeric field mode, absent/null wrapper states, unknown/null/numeric wire rejection, unchanged receiver on failure, and undeclared-zero rejection. Every consumer file was byte-identical after repeated generation. Sources and transcript retained in v1-wave2/ephemeral/wave2/enum-consumer and enum-consumer-proof.txt.
proof: npm ci followed by npm run check passed; site link check validated 17 HTML pages. Final GOFLAGS=-p=2 go test ./... passed after documentation changes. Independent review begins at this checkpoint; CLI capability integration remains pending.

review finding: Typed aliases in separate const declarations were omitted because both enum-discovery paths inferred type membership from declaration-local syntax. Resolve all enum constants from `go/types` identity in source order so aliases reach the existing duplicate-value policy.
review finding: Registered enum fields carrying `json:",string"` could produce a schema/codec disagreement in numeric mode and silently discard the option in adapted string mode. Reject the option for both enum modes and both registration entrypoints before artifact writes.
proof: Focused builder/syntax regressions passed for cross-declaration aliases and method/free-function registrations in numeric/string modes. Every negative case preserved the preexisting schema and generated-source sentinel bytes.
proof: Three complete generated-artifact snapshots, before and after two `GOFLAGS=-p=2 go generate ./...` runs, retained SHA-256 aggregate `ddaa01dec052c7c5ab5227dbe752abeae46f8d0bd0b121c8f520b76bca52e0d5`.
proof: A branch-built generator ran twice against the published-v1.0.0-rc.3 enum/union consumer without changing any consumer byte (aggregate `2c1d4090a6e4f82c3f0c9c6705c48dec27d871a61eaa8e680f903da361e7b3c4`); `go test -v ./...` passed schema-valid value/pointer enum and mixed-union roundtrips.
proof: Website `npm run check` built 13 pages, indexed and checked 17 HTML files, and found no broken internal links. `GOFLAGS=-p=2 go test ./...` passed after all product and documentation changes.
review hygiene: Removed trailing spaces from the committed website proof transcript identified as formatting noise in round one.

correction: The requested codec architecture is one containing-owner alias/RawMessage wrapper with field-specific enum and sealed-union adapters, not a generalized serializer. The current emitted source already follows that model; audit scope is generator-only redundancy.
review: Template projections (`OwnerCodecs`, `Interfaces`, and `YAMLTypes`) are append-only mutable `SchemaBuilder` state even though they are derived only for `RenderGoCode`; a repeated render can duplicate declarations. Several compatibility fields feeding this projection are now unread after hashed union helper identity and shared enum plans landed.
decision: Recommend a fresh local render view plus removal of provably unread generator fields/helpers. Preserve field-specific union helper identity, owner/promotion collision checks, embedded access paths, enum `Adapted` gating, and import alias allocation as load-bearing guards.
proof: Audit-target baseline `GOFLAGS=-p=2 go test ./...` passed at `8d8c716`. Reference `v1-union-inspection` tests failed under its owned paused edits (known Optional message assertion plus `test2-indirecttypes` invalid pointer receiver stubs); no reference-worktree files were changed.
proof: Post-audit `GOFLAGS=-p=2 go test ./...` passed; the audit changed only the review and worklog operating artifacts.
decision: Parent approved the bounded generator cleanup. `RenderGoCode` now builds a fresh `schemaTemplateData` value instead of mutating template-only slices on `SchemaBuilder`; the owner wrapper, union type switches, enum adapters, helper identity, collision/promotion checks, embedded paths, discriminator/nil guards, and import allocation are unchanged.
proof: A same-builder repeated-render regression uses the mixed union/enum fixture with YAML enabled and requires byte-identical first/second source plus exactly one owner MarshalJSON, UnmarshalJSON, and UnmarshalYAML declaration.
proof: Focused uncached codec tests passed. Two full `GOFLAGS=-p=2 go generate ./...` runs preserved tracked generated-corpus aggregate SHA-1 `127ce7859fd452217def2c5891134a3460c6f9d2`; only the three intended generator/test/template files remained modified. Final uncached `GOFLAGS=-p=2 go test -count=1 ./...` passed.
friction: Running the required baseline in the paused union reference worktree left failed-test output and temporary fixture directories. Initial status showed only its owned `builder.go` edit, so tracked outputs were restored and exact temporary directories moved to `/tmp`; avoid `path` as a zsh loop variable because it overwrites command lookup.
review acceptance: Independent review of `489851c` inspected the full diff, removed-symbol use, owner wrapper/type-switch template, collision guards, repeated-render regression, generated-artifact proof, and fresh tests; it reported no findings. Parent read and accepted the review.
