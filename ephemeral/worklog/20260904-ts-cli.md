# TypeScript CLI and artifact integration

decision: TypeScript lowering, rendering, filename validation, and ownership collision checks happen before schema or TypeScript output mutation; this keeps lowering and predictable collision failures non-destructive.
decision: `--no-changes` compares requested TypeScript files independently of schema checks, including missing or stale `types.ts` and owned barrel removal, without creating or modifying the TypeScript directory.
decision: A TypeScript artifact is generator-owned only when its bytes begin with `typescript.GeneratedHeader`; disabling the barrel removes an owned `index.ts` and preserves an unowned one.
friction: Live CLI proof against a fresh nested module failed with a blank loaded package name because `syntax.Load` evaluated the target path from the invocation directory -> copy the package config, set `Dir` to the absolute target, and load `.` without changing process CWD so relative TypeScript paths retain invocation semantics.
decision: `syntax.Load` distinguishes existing filesystem directories from recursive import-path loads; directory targets use target-local package configuration while import paths retain the established resolver behavior.
proof: Full `go test ./gen-jsonschema -count=1` and `go test ./internal/syntax -count=1` pass; focused `internal/builder` TypeScript tests pass. Coordinator live proof `ephemeral/typescript-generation/proof/run-aPjQxM` passed all nine CLI, ownership, determinism, compiler, exhaustiveness, and fresh-consumer claims.
decision: `llms.txt` now presents TypeScript declarations as an optional structural artifact, documents invocation-CWD path resolution, ownership and barrel behavior, no-change coverage, a type-only import, and the same runtime/numeric/provider/custom-codec limits as README.
correction: User assigned the root agent to coordination only and transferred final integration, proof, generated-artifact reconciliation, and checkpoint responsibility to this agent.
proof: After the adapter preserved field-local enum descriptions, serialized full Go suites passed before and after generation; ordinary generation was byte-identical across two runs; environment no-change generation preserved the same 150-artifact manifest; TypeScript proof run-X5rTzB passed all 11 claims; golangci-lint reported 0 issues.
review: Round 03 in native session 9c884e7a-c641-46da-aafa-65207fa8e2f1 ended with `only nitpicks remain`; consensus accepts the definition of done and authorizes checkpoint closeout.
