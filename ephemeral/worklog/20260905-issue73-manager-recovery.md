# Issue 73 manager recovery

correction: User requires Claude development through Tractor and fresh Claude review sessions; manager owns scope, adjudication, proof, and shipping. Prior handoff is unverified evidence, not authority. Only issue 73 is authorized in this session.
baseline: Root go test ./... passed. Existing task worktree continued; prior docs run 88352a65cb8263b50bc5c17a5030215a independently confirmed completed with exit 0. No competing writer launched.
decision: Full issue authority captured in ephemeral/issue-73/manager-review/authority.json. Review whole implementation against that authority and supported use, not prior manager claims. Retain existing work until evidence warrants a bounded correction.
proof: Task worktree go test ./... passed. Built CLI scaffold emits Declare(Person.Schema); actual go generate produces required name:string schema and compiling external-module consumer under manager-review/scaffold.
finding: Existing llms.txt still teaches legacy syntax. API reference missing Declare. Website workflow builds gomarkdoc under Go 1.24.
correction: Prior report claimed gomarkdoc required upstream repair/replacement. Reproduced parse failure, inspected binary (built go1.25.0), rebuilt identical v1.1.0 with GOTOOLCHAIN=go1.27.1, and successfully generated full Declaration documentation. Fix CI toolchain selection, not documentation architecture.
review: Fresh Claude review dc4cfe095d4dd59689962aaffab7e9aa found provider-shape mismatch panic (also pre-existing legacy behavior) and missing fluent diagnostic tests. Accept fluent-only source-positioned rejection and focused tests; reject broader legacy or callable-support expansion.
proof: Fresh Claude consumer validation a78b4cb989b3b0879c13d0a3788d4c08 demonstrated scaffold/runtime schema, provider execution incl pointer roots, ref validation, and enum/union codec roundtrips. Its attempted removal of global enum registration was not equivalent; retain that registration and document compatibility rather than add an API.
friction: Both reviewers ran mutating generation/lint despite read-only product scope, reverted incidental edits, and confirmed original baseline file set. Final validation must be uncontended. Consumer validator removed proof tests; require retained executable evidence at closeout.
decision: Bounded corrections recorded in ephemeral/issue-73/finish/adjudication.md. Documentation pass missed llms.txt and generated API/CI failure; manager requires these before shipping.

## Closeout pass (this session)

baseline: `go test ./...` green before starting; adjudication.md is authority for scope.
fix: internal/syntax/fluent_expr.go's parseFluentChainOptions now rejects, with a source-positioned error, a fluent .Accessor/.Method given a free function, a fluent .Function given a method expression, and providerRef's matched=false case (a method expression whose receiver doesn't match what the chain expects, e.g. Function(fieldOfOtherType, OtherType.Method)) - the latter added after a mid-turn correction pointed out the first pass only closed the isMethod-mismatch branch, not the matched=false branch of the same finding. providerRef's incorrect "unreachable in practice" doc comment corrected. Legacy WithStructAccessorMethod/WithStructFunctionMethod/WithFunction path untouched and reverified silently accepting the same input (repro_legacy_accessor_free_func still exits 0).
test: new scanner test (internal/syntax, standalone fluent_field_mismatch fixture - kept out of the shared typescanner package since LoadPackage eagerly parses every marker and would break the happy-path tests), two new builder/command tests (internal/builder/fluent_declaration_test.go), and - after a mid-turn correction that the first three don't exercise the actual CLI - one new subprocess test (gen-jsonschema/main_test.go, via the existing testutils.RunCommand helper) running the real built command against the same fixture and asserting exit code + stderr file:line.
fix: website-pages.yml's Set up Go step now uses go-version-file: go.mod (matching go.yml/typescript.yml), not a hardcoded 1.24.x - corrected mid-turn from an initial `../go.mod` guess (defaults.run.working-directory does not affect a `uses:` action's own file-input resolution, only `run:` steps). Regenerated website/src/content/docs/api/index.md for real (GOTOOLCHAIN=go1.27.1 gomarkdoc) and confirmed `cd website && npm run check` (build + link check, 17 pages) passes end to end.
fix: llms.txt converted to teach Declare(...) as primary throughout (scaffold, enums, interfaces, AsRef->Ref, providers, registration table), matching the pattern already used in README/website docs; each converted section keeps one "Migration:" note.
fix: enum migration notes (README.md, llms.txt, website enums.md, skill registration-api.md, CLAUDE.md) corrected to say field-level .Enum/.StringerEnum is not a full replacement for shared-type NewEnumType[T]() - the validator's ts-fluent-fixture finding. Fixed that fixture itself (manager-validation/ts-fluent-fixture/fluent/schema.go) to keep NewEnumType[Status]() and drop the field-level .Enum calls that were incompletely compensating for it; regenerated both legacy/ and fluent/ sides and confirmed all JSON/TS artifacts are now byte-identical (Composition.json/types.ts previously differed).
proof: recovered the validator's two discarded runtime smoke tests verbatim from manager-validation/run/events/000001-validate.jsonl (events 62, 87) and committed them as permanent files under the same fixture directories (scaffold-demo, pointer-provider-fixture) rather than reinventing them.
decision (mid-turn, user-directed): unify AGENTS.md/CLAUDE.md. Discovered the tracked file was actually `Agents.md` (mixed case; git is case-sensitive, the working filesystem isn't) sitting alongside a separate plain `CLAUDE.md`. Merged Agents.md's existing rules (verbatim, first) with CLAUDE.md's project guidance (Declare-updated Registration System section) into one file, corrected two claims already stale before this pass (Optional[T]+omitzero, not omitempty; ValidateJSON is --validate opt-in with a stub, not automatic), renamed to the exact uppercase AGENTS.md via a two-hop `git mv` (macOS case-insensitive fs can't case-rename in one hop) followed by `git add` to force the index to the merged content (the first git mv hop staged the pre-merge blob), and replaced CLAUDE.md with a relative symlink -> AGENTS.md. Staged (git add), not committed.
decision (mid-turn, user-directed): the primary-example migration was still incomplete. Converted examples/{basictypes,structs,indirecttypes,enums,enums_stringmode,uniontypes}/schema.go to Declare(...)/.StringerEnum/.Interface with explicit derived Impl discriminators; kept examples/enums's two NewEnumType calls (Status is shared with a bare slice-element type with no fluent attachment point) with a concise inline label; left examples/{iota_global,template_rendering,self_contained,test_options} untouched as internal legacy-limitation-demonstration fixtures (each documents a specific bug/gap in its own comments, not a style choice). Regenerated all six; only examples/uniontypes/jsonschema_gen.go changed, and only method-declaration order (verified byte-for-byte on the actual JSON).
friction: `just lint`'s staticcheck step failed on two pre-existing, untracked reviewer scratch files under ephemeral/issue-73/manager-review/scratch/ (unused-func lint, not product code, not introduced this pass). Manager resolved by relocating them to ephemeral/issue-73/manager-review/testdata/scratch/ (Go tooling skips testdata/); re-ran `just lint` clean end to end (staticcheck, govulncheck, golangci-lint all 0 issues). goimports again touched the same historical/unrelated files two prior passes already flagged (ephemeral/codec-integration-consumer/*, ephemeral/typescript-generation/proof/*/consumer/*, examples_regenerate_test.go, internal/builder/typegrammar.go, json_schema_helpers_test.go, tests/typescript/fixture/*); reverted all, confirmed pre-lint git status restored exactly, go.mod/go.sum untouched.
proof: full `go build ./...`, `go vet ./...`, `gofmt -l`, `go test ./...` (whole repo, incl. every new test above), `go run ./internal/cmd/doc-gen -check`, `cd website && npm run check`, and `just lint` all green at closeout. Full report: ephemeral/issue-73/finish/result.md.
cleanup: Moved independent-review repro fixtures from manager-review/scratch to manager-review/testdata/scratch, preserving evidence while excluding deliberate invalid/unused fixtures from root package discovery. Earlier report paths refer to their original location. This resolves the reported scratch-only staticcheck blocker without changing product code.
proof: Manager final go test -count=1 ./... passed after repository-wide regeneration. Regeneration and no-change mode produced no staged-product drift. Existing npm test TypeScript conformance passed. Retained standalone scaffold and pointer-provider tests passed with portable relative module replacements.
proof: Corrected parity fixture module identities to the same example.com/issue73-parity; rebuilt final CLI and regenerated both. JSON schemas, TypeScript types/barrel, and generated Go are all byte-identical. Different prior module identities explained private Go helper hash differences; no product fix was warranted.
proof: Built CLI invalid field-association diagnostic independently confirmed nonzero exit and fixture.go:line:column. CLAUDE.md -> AGENTS.md and content identity verified. User-requested canonical instructions include preserved rules and current fluent guidance.
review: Fresh complete final Claude review started as Tractor 979abc48bc2a7e1dad474ed69bb4ceca, with prior verdicts excluded and no product mutations permitted.
checkpoint: Final product checks passed, including website rebuild after enum godoc caveat. Preparing a draft PR to start CI while the fresh final reviewer completes; merge remains gated on review and checks.
