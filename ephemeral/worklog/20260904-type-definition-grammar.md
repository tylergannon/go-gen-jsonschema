# Authoritative type-definition grammar

correction: User requested the authoritative Go types for the accepted definition grammar as the first implementation step after TypeScript research. Scope is the grammar, its invariants and tests; no TypeScript emitter or #57/#71 codec work.
decision: Start from refreshed origin/main 604fabb in codex/type-definition-grammar. The v1 contract (#69) has merged since the research handoff; use the accepted contract and current source instead of the draft snapshot.
proof: Root baseline `go test ./...` passed before any repository work. New grammar behavior will be demonstrated through its Go API with accepted/rejected definition graphs and meaningful unit tests; existing CLI generation behavior is outside this change.
decision: Keep the backend-independent grammar in an internal package. Closed Go node interfaces define constructors; validation enforces graph/context invariants that Go's type system cannot express. Do not make existing generation claim to consume the grammar until a later lowering step is implemented.
correction: Current source accepts explicit empty union tags and distinguishes value T from pointer *T implementations. Preserve both; duplicate exact registrations or tags still reject. An empty raw discriminator key defaults before entering the resolved grammar.
correction: Ordinary EnumValues is demonstrated inside slices/pointers by existing fixtures. Restrict only EnumNames adaptation to direct contexts; a blanket nested-enum rejection would discard accepted constraints.
decision: A discriminator property already in a payload is a contextual refinement when its type admits the selected tag. Preserve shared payload definitions; reject impossible collisions. Runtime custom-hook compatibility remains #57/#62/#71 evidence.
decision: Future scanner lowering must validate syntax/source facts before normalization; this grammar alone cannot distinguish a forbidden wrapper alias or parenthesized interface spelling. The grammar is intentionally not connected to CLI generation in this first step.
review: Independent adversarial review identified anonymous-object EnumNames adaptation as an admitted but unsupported ownership context. Source parser only accepts named Receiver{}.Field registrations (scan_expr.go:350-364,438-441). Accepted the finding, restricted the direct-field enum context to named owners, and added paired negative/positive owner-context tests. EnumValues composition remains accepted.
friction: A lint invocation raced the test agent's in-progress file and returned transient syntax errors. Later checks must wait for file ownership handoff. Settled test lint also flagged intentionally ignored diagnostic-returning test helpers; explicit discard fixes those without weakening assertions.
proof: Final serial `go test ./...` passed after all product and test edits; log at ephemeral/type-grammar/final-go-tests.log. Package golangci-lint reports 0 issues. API tests cover exact constants, field-local union tags/pointer identity, DAG sharing/back edges, ownership-sensitive memoization, wrappers, nil/foreign constructors and source-aware diagnostics.
review: Independent follow-up verified the named-owner fix and Required/Optional/Nullable shared-node regressions; updated outcome is no findings in ephemeral/reviews/20260904-type-grammar-adversarial-review-round-01.md.
friction: Independent full Go suites temporarily modify the same builder test_run paths. Run subsequent complete suites serially; do not interpret transient fixture deletions during execution as task edits. Final status contains only task-owned new files.
state: Grammar-only implementation complete on codex/type-definition-grammar, based on604fabb. Prepare a local checkpoint commit; no push, PR, merge, scanner adapter or emitter is part of this step. The original three inspiration files remain untracked in the separate research worktree.
