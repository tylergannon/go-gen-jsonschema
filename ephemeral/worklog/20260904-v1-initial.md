# v1 initial implementation

Scope: user authorized recording milestone sequencing/native dependencies and beginning the initial batch, with primary-agent problem solving and lower-model subagent implementation.

Ownership: root owns contract #56, GitHub plan, integration and review. GPT-5.6 Luna workers own #33 fixture registration, #34 stdout removal, and #59 helper fixes in separate worktrees. No automatic merge/release is part of this batch.

Baseline: root go test ./... passed on 4376ce0; root fast-forward confirmed current origin/main. Existing untracked .claude and .codex directories left untouched.

Decision: native GitHub dependencies represent completion gates. Milestone description records permitted parallel preparation and the three implementation tracks. #58 follows #57's common generated-method architecture. Readback is stored in ephemeral/github/verified-dependencies.json.

Correction: closed #36 as fixed by #38 after previous-turn cold-module-cache evidence. Removed its stale unresolved prerequisite from #66.

Decision: manual Strict means all declared properties required plus additionalProperties=false, overriding explicit Required/AdditionalProperties in both helper types. Empty EnumSchema keeps the SchemaNode signature and produces a descriptive MarshalJSON error. Delegated both to helper worker after resolving policy.

Open decision: asked maintainer whether to retain existing provider APIs, replace with typed props (#27), or remove parameterized schemas before 1.0. Recommendation is retain for 1.0; no removal/replacement is assumed.

Friction: three simultaneous full Go baselines briefly exhausted OS file descriptors and prevented root command launch. Workers instructed to bound subsequent build parallelism using GOFLAGS=-p=2 where needed; independent worktrees avoid generated-fixture write races.

Finding: the old v1 draft asserted unsupported API signatures and deferred enum codecs past v1, contradicting current release direction. Replaced it with a target contract/support matrix, not an assertion that all future work already exists.

Finding: source renders time.Time as string plus descriptive RFC3339 guidance, without JSON Schema format enforcement. Unresolved external types can currently map permissively; byte-like slices and json:,string have standard JSON encoding behavior that a generic scalar/container schema claim does not establish. Contract marks these cases unproved/excluded and assigns explicit diagnosis/conformance to #60/#62.

Review: root accepted #34's real missing-type/no-stdout regression and #33's restored string-mode fixture, generated schema/accessor goldens and runtime accessor inspection. Both worker commits cherry-picked into initial integration branch. #33 is schema-generation proof, not enum codec proof; #58 remains open.

User correction: other agents owned #33/#34. Live refresh found both already merged (#67/#68), with main now 76658ed. Dropped our duplicate #33/#34 commits and rebased only helper #59 onto current main; preserved the contract/planning artifacts. No duplicate PRs or issue closures were published for #33/#34. Read-only worker reviews continue; implementation scope is now #59 plus #56 contract/planning.

Independent contract review: reviewer identified legacy discriminator name/suffix inconsistencies, insufficiently concrete custom-hook failure stages, and an incomplete enumeration of rejected interface forms. Resolved in the draft with collision rejection/shared per-field mappings, explicit generation-time versus runtime hook examples, and a closed interface-shape set plus promoted-field proof requirement. These are target requirements assigned to #57/#60/#62, not claims that legacy paths already conform.

Contract review closeout: independent reviewer confirmed all three material findings resolved. Contract/planning artifacts isolated onto codex/v1-contract from current main76658ed; helper fix remains separate. The #56 provider decision remains pending; contract PR will be draft.
