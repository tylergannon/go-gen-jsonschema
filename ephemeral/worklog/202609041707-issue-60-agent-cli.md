# Issue 60 agent CLI worklog

decision: Machine discovery uses `version --json` and model diagnosis uses `inspect --json`; both call reusable domain operations and share a versioned result envelope.
decision: Exit 0 means the requested inspection is fully supported, 2 means the request is invalid, 3 means a valid model contains unsupported or unknown shapes, and 1 means an internal or toolchain failure.
decision: Capability claims describe only installed behavior; pending union and string-enum codecs remain unadvertised until their implementations are integrated and proven.
decision: Per-type capability results distinguish schema, JSON encode/decode, validation, and YAML input; aggregate status includes both diagnostics and capability-limited type status.
decision: Inspection loads with `-mod=readonly`, maps each selected registered root independently through the builder, and never renders, invokes providers, or writes source/module files.
correction: Provider limits apply whenever provider fields are configured, including templates without `WithRenderProviders`; detection uses resolved builder provider metadata.
correction: Byte-like slice detection resolves named uint8 aliases; inline structs respect ignored JSON fields and diagnose `json:,string` rather than bypassing their tags.
correction: English error parsing was rejected; package/scanner locations and classifications use typed errors, while unclassified builder failures are reported as unknown.

decision: Production-only JSON hook discovery reuses `syntax.FindProductionJSONMethods` from union codec commit `ebb76d7`; hook diagnostics point at the active production method rather than the generation stub/package.
correction: Inline struct JSON exclusions and modifiers must be evaluated at the inline field, and `[]MyByte` is byte-like when `MyByte` has underlying `uint8`.
decision: Missing imports are toolchain failures based on the typed package graph (an import is absent from the loaded import map), while parse/type errors in an otherwise resolved target are invalid-model failures.

proof: A clean built CLI reported pseudo-version `v1.0.0-rc.3.0.20260904233159-a430522455e8`, revision `a430522455e82e92c0395b8f12ba78a1cd08e092`, and `modified:false`; it did not guess `latest`.
proof: In fresh module `/tmp/issue-60 consumer.HZcizf/model`, machine inspection returned exit/status 0/supported, 3/unsupported (`unsupported_json_string`, `unsupported_base64_bytes`), 3/unknown (`unknown_custom_json_hook`, `unknown_external_type`), and 3/unknown for an unregistered type; invalid flags returned 2/invalid and missing dependency returned 1/error toolchain.
proof: Every machine case emitted parseable JSON with zero stderr bytes. File hashes before/after the model cases matched, and readonly inspection did not create a missing `go.sum` in the broken module.
correction: Production hook keys must include package path and generation-only stubs must be excluded; inspection now scans active production files per traversed package and tests a same-name remote type.
decision: `--help` is successful and useful on both commands; JSON help returns one versioned envelope with `usage`, while human help uses stderr. Operation panics are recovered at the CLI/domain boundary into a single `internal_operation_panic` machine result.

Final `GOFLAGS=-p=2 go test ./...` passed after all code and test changes. Final clean consumer proof used the exact source tree subsequently committed; no source/module files changed during inspection.
proof: Durable consumer sources, rerunnable proof script, exact machine summaries, executable provenance, and hash assertions are stored under `ephemeral/issue-60/`; `ephemeral/issue-60/prove.sh` reproduced the full matrix after capture.

Baseline `GOFLAGS=-p=2 go test ./...` passed at `71ceca8d8ff0075471070a59ad886f120971383d`.

parent integration: added docs/agent-cli.md plus README, llms, shipped-skill and website routing for the versioned machine contract. Existing installations check global help first because legacy CLI can treat unknown subcommands as generation requests. Reproducible consumer runs now keep scratch files under ephemeral as required by AGENTS and quote the executable path.

correction: Valid generic model syntax is an unsupported model boundary, not a package/toolchain failure. Generic expressions now carry typed `unsupported_generic_type` scanner diagnostics at the source position; untyped scan failures conservatively report `scan_unclassified`/unknown rather than guessing from English text.
correction: Builder mapping no longer writes unrecognized-node or stack debug output to stdout. Both paths return contextual errors so machine mode retains its one-JSON-document guarantee.
correction: Generation package loading now combines caller `GOFLAGS` tags with the reserved `jsonschema` tag. Production JSON-hook discovery preserves caller tags but removes the reserved tag so declaration stubs remain excluded even when `GOFLAGS` contains `-tags=jsonschema`.
proof: The parent-built CLI reproduced the generic defect as exit 1, `package_load_failed`/toolchain, with zero stderr. A binary built after the fix inspected the same consumer as exit 3, `unsupported_generic_type`/unsupported at `types.go:3:20`, with zero stderr and exactly one JSON document.
proof: Focused scanner/domain/CLI tests and final `GOFLAGS=-p=2 go test ./...` passed after the boundary, build-context, and stdout fixes.

correction: Reading only process `GOFLAGS` misses settings saved in `GOENV` and can silently erase custom tags when the loader supplies its generation tag. Inspection now resolves effective `GOFLAGS`, `GOOS`, `GOARCH`, and `CGO_ENABLED` once through read-only `go env -json`, then shares that immutable context across package loading, remote scans, root mapping, and production-hook discovery.
proof: Against the parent's isolated `GOENV` consumer, the pre-fix binary returned exit 0/supported despite a custom-tag production `MarshalJSON`. The rebuilt CLI returned exit 3/unknown with `unknown_custom_json_hook`, zero stderr, and one JSON document. Unit and CLI regressions use temporary GOENV files and never write global Go configuration; malformed GOFLAGS yields explicit `build_context_unavailable`/unknown.
proof: Final `GOFLAGS=-p=2 go test ./...` passed after effective GOENV/platform context was threaded through the inspection operation.

parent proof: Rebuilt ed27f05 with pending documentation changes and reran the durable consumer matrix. All expected statuses/exits passed; stdout was one JSON document, stderr empty, all consumer hashes unchanged, and no broken-module go.sum created. Independent saved-GOENV and generic consumers both returned exit 3 with precise unknown_custom_json_hook / unsupported_generic_type diagnostics. Review round 2 is in progress; branch remains a draft until review and CI converge.
