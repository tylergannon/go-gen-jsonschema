The 1.0 milestone had accumulated several large agent-infrastructure and proof programs that did not directly improve initial adoption. This records the value-based triage applied to GitHub: concrete correctness first, the typed fluent authoring API and bounded Codex plugin next, representative consumer proof, then legal/release closeout.

The machine protocol, full preview subsystem, and model-backed evaluation harness are explicitly post-1.0. Issue #80 preserves the practical silent-registration bug extracted from the larger fluent proposal.

Validation: `go test -count=1 ./...`; pre-commit golangci hook.
